<!DOCTYPE html>
<html lang="en">

  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Review Edit Form</title>
    <link rel="stylesheet" href="style.css">
  </head>

  <body>
    <div class="pets_container">
      <nav>
        <a href="/">HOME</a>
        <a href="/shelters">SHELTERS</a>
        <a href="/pets">PETS</a>
      </nav>

      <main>
        <div class="child">
          <h1>Edit Review</h1>

          <div class="form-container">

            <%= form_tag "/reviews/#{@review.id}", method: :patch do %>
              <%= label_tag :title %><br/>
              <%= text_field_tag :title, "#{@review.title}", :maxlength => 25, :size => 20 %><br/>
              <%= label_tag :rating %> (1 through 5)<br/>
              <%= number_field_tag :rating, "#{@review.rating}", min: 0, max: 5%><br/>
              <%= label_tag :content %><br/>
              <%= text_area_tag :content, "#{@review.content}" %><br/>
              <%= label_tag :optional_picture %><br/>
              <%= text_field_tag :optional_picture, "#{@review.optional_picture}" %><br/>
              <br>
              <%= submit_tag "Update Review" %>
            <% end %>
          </div>
        </div>
      </main>
    </div>
  </body>
</html>
